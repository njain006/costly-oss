/**
 * MOVED — see e2e/specs/00-api-smoke.spec.ts and
 * e2e/specs/03-demo-overview.spec.ts.
 *
 * This file is kept (empty) so existing references / docs that point at
 * `e2e/demo-dashboard.spec.ts` don't 404. Playwright's `testMatch` now
 * scopes test discovery to `e2e/specs/*.spec.ts`, so this file is
 * intentionally not executed.
 */
export {};
